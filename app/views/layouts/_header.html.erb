<nav class="navbar navbar-fixed-top navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= image_tag("rocket.png", alt: "Rocket logo", id: "rocket-logo", class: "navbar-brand") %>
      <%= link_to "Rocket Supply", root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Companies", companies_path %></li>
        <li><%= link_to "About", "#" %></li>
        <li><%= link_to "Help", "#" %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Products <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "All products", products_path %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Anchors", "#" %></li>
            <li><%= link_to "Pumps", "#" %></li>
            <li><%= link_to "Packers", "#" %></li>
          </ul>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
        <li><%= link_to "#{current_user.email}", "#" %></a></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Account<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% if current_user.companies.size > 0 %>
              <% current_user.companies.each do |c| %>
                <li><%= link_to "#{c.name}", c %></li>
              <% end %>
            <% end %>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Manage account", edit_user_registration_path(current_user) %></li>
            <% if current_user.companies.size > 0 %>
              <li><%= link_to "New product", new_product_path %></li>
            <% end %>
            <li><%= link_to "New company", new_company_path %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Sign out", destroy_session_path(current_user), method: :delete, confirm: 'Are you sure?' %></li>
          </ul>
        </li>
        <% else %>
        <li><%= link_to "Sign in", new_user_session_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>